## Lab 7

En-Yu Hsu, Abas Mohamed

# Question 1

I will fit automated tests within a Github action that runs whenever code is pushed.
I think this is the best because the tests will run automatically when the someone pushes the code. This will help us catch the bug earlier and before the code is merged. But this also prevent people from forgetting to run them.

# Question 2

No, I will use a unit test because unit tests are designed to test small pieces of code.

# Question 3

The difference between snapshot and navigation mode of lighthouse is that the navigation mode gives more metrics to start and seems to focus on initial load, while snapshot captures current state. Snapshot does not reload the page.

# Question 4

If we want our site to be better reachable from the web we can add more discriptive meta tags. Secondly we can add a lang attribute to make sure we are not assuming someone page is in english where it may not display correctly. Lastly, if I were to visit the site after around 10 minutes the cache will clear and the load time will take the same time as the initial. So by extending the cache clear time the load can stay fast for longer.
